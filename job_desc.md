# 숙제관리 기능별 업무 로직 정의서

> 최초 작성일: 2026-08-29
> 최종 갱신일: 2026-09-03
> 문서 상태: 현재 구현 기준 및 향후 기능 정의
> 적용 원칙: 향후 기능 및 구현은 이 문서를 먼저 수정한 후 코드에 반영한다.

## 0. 공통 정의

### 0.1 사용자 역할

| 구분 | 정의 | 데이터 권한 |
|---|---|---|
| 학생 | 숙제와 과목 데이터의 주 소유자 | 자신의 숙제 조회·등록·수정·삭제 |
| 학부모 | 승인된 학생을 지원하는 부 사용자 | 연결된 학생의 동일한 숙제 조회·등록·수정·삭제 |

학생과 학부모가 각각 숙제 사본을 가지지 않는다. 숙제는 항상 학생 한 명에게 귀속되며, 학부모가 입력한 숙제도 연결 학생의 숙제로 저장한다.

### 0.2 주요 식별자

| 명칭 | 의미 | 예시 |
|---|---|---|
| `actorId` | 서버 세션에서 확인한 현재 로그인 사용자 ID | 학부모가 수정하면 학부모 ID |
| `studentId` | 조회·저장 대상 데이터의 소유 학생 ID | 학부모와 연결된 학생 ID |
| `homeworkId` | 숙제 ID | 수정·삭제 대상 식별 |
| `selectedDate` | 화면 달력에서 사용자가 선택한 날짜 | 숙제 등록 기본 배정일 및 목록 필터 |
| `assignedDate` | 숙제가 달력에 매핑되어 표시되는 배정일 | `homeworks.assigned_date` |
| `dueDate` | DB에 저장되는 숙제 마감일 | `homeworks.due_date` |

`actorId`와 `studentId`는 같은 값일 수도 있지만 역할이 다르므로 코드와 서버 내부 로직에서 구분한다. 클라이언트는 `actorId`를 전달하지 않으며 서버가 인증 세션에서 결정한다.

### 0.3 공통 처리 원칙

1. 화면 상태만 신뢰하지 않고 서버가 사용자 역할과 연결 상태를 다시 검증한다.
2. 학부모는 `LINK_APPROVED` 상태인 학생 데이터만 관리할 수 있다.
3. 숙제 등록·수정·삭제와 변경 이력 저장은 하나의 트랜잭션으로 처리한다.
4. 삭제는 물리 삭제 대신 소프트 삭제를 기본으로 한다.
5. 목록 API는 `is_deleted = false`인 데이터만 반환한다.
6. 모든 날짜는 브라우저와 서버의 시간대 차이를 피하기 위해 `YYYY-MM-DD` 형식의 날짜 값으로 전달한다.
7. `actorId`는 요청 본문이나 쿼리 파라미터로 받지 않고 서버 HTTP 세션의 인증 사용자에서 추출한다.

---

## 1. 로그인

### 1.1 목적

사용자를 인증하고, 로그인 사용자의 역할과 관리 가능한 학생 범위를 결정한다.

### 1.2 입력

- 로그인 ID
- 비밀번호

### 1.3 처리 흐름

1. 사용자가 로그인 ID와 비밀번호를 입력한다.
2. 서버는 로그인 ID로 사용자를 조회한다.
3. 저장된 비밀번호 해시와 입력 비밀번호를 비교한다.
4. 비활성·잠금·탈퇴 사용자인지 확인한다.
5. 인증 성공 시 사용자 ID와 역할을 포함한 세션 또는 토큰을 생성한다.
6. 학생이면 자신의 학생 ID를 기본 관리 대상으로 설정한다.
7. 학부모이면 승인된 연결 학생 목록을 조회한다.
8. 연결 학생이 한 명이면 해당 학생을 기본 관리 대상으로 설정한다.
9. 연결 학생이 여러 명이면 첫 학생을 기본 관리 대상으로 설정하고 대시보드에 학생 선택기를 표시한다.
10. 연결 학생이 없으면 연결 안내 화면으로 이동하며 숙제 관리 기능은 비활성화한다.

### 1.4 출력 및 화면 이동

- 학생: 자신의 숙제 대시보드
- 연결 학생 1명인 학부모: 해당 학생의 숙제 대시보드
- 연결 학생 여러 명인 학부모: 기본 학생의 숙제 대시보드와 관리 학생 선택기
- 연결 학생이 없는 학부모: 사용자 매핑 안내 화면

### 1.5 오류 처리

- 존재하지 않는 ID와 잘못된 비밀번호는 동일한 인증 실패 메시지로 처리한다.
- 반복 실패 시 계정 잠금 또는 지연 정책을 적용한다.
- 인증되지 않은 API 요청은 HTTP 401로 처리한다.
- 연결되지 않은 학생 접근은 HTTP 403으로 처리한다.

### 1.6 현재 구현 상태

- Spring Security와 서버 HTTP 세션 기반 로그인이 구현되어 있다.
- 로그인 ID는 대소문자를 구분하지 않고 조회하며 비밀번호는 BCrypt 해시와 비교한다.
- 로그인 성공 시 세션 ID를 변경하고 인증 사용자를 서버 세션에 저장한다.
- 인증 세션 복원, 로그아웃 및 CSRF 토큰 발급 API가 구현되어 있다.
- 5회 연속 로그인 실패 시 계정을 15분 동안 잠그며, 성공 시 실패 횟수와 잠금 상태를 초기화한다.
- 세션 만료는 30분이며 쿠키에 HttpOnly와 SameSite=Lax 설정을 사용한다. 운영 HTTPS 환경에서는 `SESSION_COOKIE_SECURE=true`가 필요하다.
- 학생은 본인 대시보드로, 학부모는 승인된 연결 학생 목록을 기준으로 대시보드로 이동한다.
- 연결 학생이 없는 학부모는 로그인 상태를 유지한 채 가족 연결 안내 화면으로 이동한다.
- 학부모의 연결 학생이 한 명이면 자동 선택하고, 여러 명이면 대시보드 상단에서 관리 학생을 전환한다.
- 요청의 작업자 ID는 클라이언트 값이 아니라 서버 인증정보에서 결정한다.

---

## 2. 회원가입

### 2.1 목적

학생 또는 학부모 계정을 생성하고 이후 사용자 매핑이 가능한 상태로 만든다.

### 2.2 공통 입력

- 사용자 역할: 학생 또는 학부모
- 이름
- 로그인 ID
- 비밀번호 및 비밀번호 확인
- 서비스 약관 동의 정보
- 선택 정보: 지역, 연락처 등

### 2.3 공통 처리 흐름

1. 필수값과 형식을 화면에서 1차 검증한다.
2. 서버에서 동일한 검증을 다시 수행한다.
3. 로그인 ID의 대소문자 무시 중복을 확인한다.
4. 비밀번호 정책을 확인하고 평문이 아닌 단방향 해시로 저장한다.
5. 역할 공통코드와 함께 사용자 레코드를 생성한다.
6. 가입 완료 후 로그인 또는 추가 설정 화면으로 이동한다.

### 2.4 학생 가입 후 처리

1. 학생 사용자를 생성한다.
2. 기본 과목을 학생 소유 과목으로 생성한다.
3. 학부모 연결에 사용할 초대 코드 또는 연결 요청 수단을 생성할 수 있다.
4. 학생 본인을 기본 관리 대상으로 설정한다.

### 2.5 학부모 가입 후 처리

1. 학부모 사용자를 생성한다.
2. 연결할 학생 정보 또는 학생 초대 코드를 입력하도록 안내한다.
3. 학생 연결 전에는 숙제 데이터가 생성되지 않는다.
4. 연결 승인 후에만 해당 학생의 숙제를 관리할 수 있다.

### 2.6 트랜잭션

- 사용자 생성과 학생 기본 과목 생성은 하나의 트랜잭션으로 처리한다.
- 중간 실패 시 사용자만 생성되거나 기본 과목 일부만 생성되지 않도록 전체 롤백한다.

### 2.7 현재 구현 상태

- 회원가입 API와 화면은 미구현이다.
- `users` 테이블에는 `password_hash`, `is_enabled`, `failed_login_count`, `locked_until`, `last_login_at` 인증 컬럼이 추가되어 있다.
- 기존 테스트 사용자 ID 1~4에는 서로 다른 BCrypt 초기 비밀번호 해시가 설정되어 있으나 신규 사용자 생성 로직은 없다.
- 초기 사용자는 Flyway 마이그레이션으로 입력되어 있다.
- 가입 시 로그인 ID 중복 검사, 비밀번호 정책, 약관 동의 저장 정책은 구현 전에 확정해야 한다.

---

## 3. 사용자 매핑

### 3.1 목적

학부모가 특정 학생의 숙제를 함께 관리할 수 있도록 승인된 관계를 만든다.

### 3.2 관계 상태

| 상태 | 의미 | 숙제 접근 가능 여부 |
|---|---|---|
| `LINK_INVITED` | 연결 요청 또는 초대 대기 | 불가 |
| `LINK_APPROVED` | 학생 또는 정책에 의해 승인됨 | 가능 |
| `LINK_DISCONNECTED` | 연결 해제됨 | 불가 |

### 3.3 연결 요청 흐름

1. 학생은 가족 연결 관리 화면에서 자신의 8자리 연결 코드를 확인한다.
2. 학부모가 학생에게 전달받은 연결 코드를 입력한다.
3. 서버는 요청자가 학부모이고 코드 소유자가 활성 학생인지 확인한다.
4. 이미 승인된 동일 학생·학부모 연결은 차단하고, 기존 해제 관계가 있으면 같은 관계를 재초대 상태로 변경한다.
5. `parent_student_links`에 `LINK_INVITED` 상태로 저장한다.
6. 학생에게 승인 요청을, 학부모에게 승인 대기 상태를 표시한다.

### 3.4 승인 흐름

1. 학생이 가족 연결 관리 화면에서 연결 요청을 확인한다.
2. 학생 ID와 학부모 ID가 실제 역할과 일치하는지 서버가 검증한다.
3. 연결 상태를 `LINK_APPROVED`로 변경한다.
4. 승인 이후 학부모의 관리 학생 목록에 해당 학생을 포함한다.
5. 기존 학생 숙제를 복사하지 않고 같은 `studentId` 데이터에 접근하게 한다.

### 3.5 연결 해제 흐름

1. 연결 해제 권한을 확인한다.
2. 상태를 `LINK_DISCONNECTED`로 변경한다.
3. 학부모의 이후 조회·변경 요청을 차단한다.
4. 학부모가 과거에 작성한 숙제와 변경 이력은 삭제하지 않는다.
5. 숙제 소유권은 계속 학생에게 유지된다.

### 3.6 다중 연결 정책

- 학생 한 명은 여러 학부모와 연결될 수 있다.
- 학부모 한 명은 여러 학생과 연결될 수 있다.
- 학부모가 여러 학생과 연결되면 현재 관리 학생을 반드시 선택한다.
- 화면과 API는 현재 선택한 `studentId`를 데이터 범위로 사용한다.

### 3.7 현재 구현 상태

- `parent_student_links` 테이블과 연결 상태 공통코드는 존재한다.
- 과목 및 숙제의 조회·변경 권한은 서버 세션 사용자와 `LINK_APPROVED` 상태를 확인한다.
- 학생별 8자리 연결 코드 생성·재발급과 학부모의 연결 요청 API가 구현되어 있다.
- 학생의 요청 승인·거절, 양측의 연결 해제 API와 가족 연결 관리 fragment가 구현되어 있다.
- 승인된 학생이 없는 학부모는 로그인 상태를 유지한 채 연결 안내 화면으로 이동한다.
- 현재 사용자 인증 응답은 승인된 전체 학생 목록을 반환하며, 학부모는 대시보드에서 관리 학생을 전환할 수 있다.
- 초대 코드는 학생 계정에만 발급하며 코드 재발급 시 이전 코드는 즉시 무효화한다.
- 본문이 없는 `201 Created`·`204 No Content` 성공 응답은 프런트엔드 공통 API 모듈에서 오류 없이 `null`로 처리한다.
- 연결 요청·승인·해제와 코드 재발급은 사용자·관계 식별자를 애플리케이션 로그에 기록하되 연결 코드 자체는 기록하지 않는다.

---

## 4. 사용자 전환

### 4.1 목적

현재 로그인 사용자를 안전하게 전환하거나, 향후 학부모가 여러 연결 학생 중 관리 대상을 변경한다.

### 4.2 구분해야 할 상태

```text
현재 작업자(actorId)
현재 작업자 역할(actorRole)
현재 관리 학생(studentId)
현재 관리 학생 이름(studentName)
```

로그인 사용자 전환은 서버 세션을 종료하고 다른 계정으로 다시 로그인하는 작업이다. 관리 학생 전환은 학부모 인증 세션을 유지한 채 승인된 학생 중 데이터 범위만 변경하는 작업이다.

### 4.3 로그인 사용자 전환

1. 프로필 메뉴에서 사용자 전환을 선택한다.
2. 서버 로그아웃 API로 기존 HTTP 세션을 무효화한다.
3. 기존 숙제·과목·필터·폼과 현재 사용자 상태를 초기화한다.
4. 로그인 화면으로 이동한다.
5. 다른 로그인 ID와 비밀번호로 인증한다.
6. 새 인증 사용자의 역할과 승인 관계에 따라 다음 화면을 결정한다.

### 4.4 학부모 관리 학생 전환

1. 서버가 반환한 승인된 연결 학생 목록을 표시한다.
2. 학부모가 관리할 학생을 선택한다.
3. 현재 관리 학생 ID를 선택 학생 ID로 변경한다.
4. 숙제와 과목 URL에는 선택 학생 ID를 사용한다.
5. 등록·수정·삭제의 수행자는 서버 세션의 학부모 ID를 계속 사용한다.
6. 기존 숙제·과목·필터·폼 상태를 초기화하고 새 학생 데이터를 조회한다.

### 4.5 데이터 공유 규칙

- 학생과 학부모는 동일한 `homeworks.student_id` 데이터를 조회한다.
- 학부모가 등록한 숙제의 `student_id`는 연결 학생 ID다.
- `created_by`, `updated_by`, `deleted_by`, 이력의 `actor_id`에는 실제 수행자 ID가 저장된다.
- 사용자 전환 시 브라우저의 이전 사용자 데이터는 재사용하지 않고 API에서 다시 조회한다.

### 4.6 오류 처리

- 연결 학생이 없는 학부모는 대시보드 진입을 막는다.
- 연결이 해제된 상태에서 저장 요청을 보내면 서버가 HTTP 403을 반환한다.
- 사용자 전환 중 조회 실패 시 이전 사용자의 데이터를 표시하지 않고 오류 상태를 보여준다.

### 4.7 현재 구현 상태

- 프로필 메뉴의 사용자 전환은 기존 서버 세션을 로그아웃한 후 로그인 화면으로 이동한다.
- 브라우저 `sessionStorage` 사용자 복원은 제거되었고 `/api/auth/me`로 서버 세션을 복원한다.
- 학생 데이터 소유자 ID와 인증된 작업자 ID가 서버 로직에서 분리되어 있다.
- 학부모의 관리 학생 전환은 아직 구현되지 않았으며 현재는 승인 관계의 첫 번째 학생만 사용한다.
- 실시간 동기화는 없으며 로그인, 사용자 전환 또는 API 재조회 시 최신 DB 상태를 읽는다.

---

## 5. 숙제 등록·수정·삭제

## 5.1 숙제 등록

### 목적

현재 관리 학생에게 특정 날짜의 숙제를 등록하고 생성 이력을 남긴다.

### 선택일과 마감일 분리 규칙

```text
달력에서 선택한 selectedDate
        ↓ 폼을 열 때 기본값 설정
숙제 폼 상단의 assignedDateInput
        ↓ 저장 요청
HomeworkSaveRequest.assignedDate
        ↓ INSERT
homeworks.assigned_date
```

- 달력에서 선택한 날짜는 숙제 등록 폼 상단의 선택일 기본값으로 반드시 복사한다.
- 사용자가 폼 상단에서 선택일을 변경하면 변경된 날짜를 `assignedDate`로 저장한다.
- 마감일은 `dueDate`로 별도 입력·저장하며 달력 목록 매핑 기준으로 사용하지 않는다.
- 마감일은 선택일과 같거나 이후여야 한다.
- 저장 성공 후 화면의 선택 날짜를 저장된 `assignedDate`로 이동한다.
- 저장된 숙제는 해당 날짜 목록과 주간 개수에 즉시 반영한다.
- 서버는 `assignedDate` 또는 `dueDate`가 없으면 HTTP 400으로 거부한다.

### 입력

- 관리 대상 학생 ID
- 과목: 국어, 영어, 수학, 기타 중 하나
- 제목
- Task 목록: Task ID(수정 시), 내용
- 숙제 선택일 `assignedDate`
- 마감일 `dueDate`
- 현재 작업자 ID는 입력값으로 받지 않고 서버 세션에서 결정

### 처리 흐름

1. 사용자가 달력에서 숙제를 등록할 날짜를 선택한다.
2. 숙제 추가 버튼을 누른다.
3. 폼 상단의 숙제 선택일을 달력 선택 날짜로 초기화한다.
4. 마감일은 별도 필드로 표시하며 신규 등록 기본값은 선택일과 같다.
5. 사용자는 숙제 선택일과 마감일을 각각 변경할 수 있다.
6. 화면과 서버에서 필수값, 길이, 마감일이 선택일보다 빠르지 않은지 검증한다.
7. 서버가 작업자의 학생 관리 권한을 검증한다.
8. 과목이 국어, 영어, 수학, 기타 중 하나인지 확인하고 해당 과목 ID를 사용한다.
9. 숙제를 생성하고 Task를 입력 순서대로 함께 저장한다.
10. 신규 Task는 모두 미완료 상태로 생성한다.
11. `student_id`에는 관리 학생 ID를 저장한다.
12. `assigned_date`에는 숙제 선택일, `due_date`에는 마감일을 저장한다.
13. `created_by`, `updated_by`에는 실제 작업자 ID를 저장한다.
14. 숙제와 Task를 포함한 생성 후 스냅샷을 숙제 이력에 `HW_CREATED`로 저장한다.
15. 저장된 숙제를 다시 조회하여 응답한다.
16. 화면은 저장된 선택일로 이동하고 숙제·과목 목록을 새로 조회한다.

### 현재 구현 상태 및 점검사항

- `openForm()`에서 `assignedDateInput`을 현재 선택 날짜로 설정한다.
- 선택일과 마감일을 각각 API로 전달해 DB의 `assigned_date`, `due_date`에 저장한다.
- 저장 후 화면 선택 날짜를 폼의 선택일로 이동한다.
- 기존 데이터는 Flyway V6에서 `assigned_date = due_date`로 이관한다.

## 5.2 숙제 수정

### 목적

기존 숙제의 과목, 제목, Task 목록 또는 날짜를 변경하고 변경 이력을 남긴다.

### 처리 흐름

1. 숙제 ID로 기존 데이터를 조회한다.
2. 숙제가 없거나 삭제되었으면 HTTP 404를 반환한다.
3. 작업자가 학생 본인 또는 승인된 연결 학부모인지 확인한다.
4. 수정 전 DB 행을 JSON 스냅샷으로 저장한다.
5. 폼에는 기존 숙제의 `assignedDate`와 `dueDate`를 각각 표시한다. 달력 선택 날짜로 덮어쓰지 않는다.
6. 과목, 제목, 선택일, 마감일과 수정자를 변경한다.
7. 기존 Task ID가 유지되면 완료 상태를 보존하며 내용과 순서를 수정한다.
8. 새 Task는 미완료로 추가하고 폼에서 제거한 Task는 삭제한다.
9. 수정 후 숙제와 Task 스냅샷을 생성한다.
10. 변경 전후 값을 `HW_UPDATED` 이력으로 저장한다.
11. 변경된 `assignedDate`가 현재 선택 날짜와 다르면 화면을 새 날짜로 이동한다.
12. 숙제·과목 목록을 다시 조회한다.

## 5.3 Task 완료 및 진행률 계산

1. 사용자가 숙제 카드의 Task 체크박스를 변경한다.
2. 서버는 Task와 소속 숙제의 존재 여부, 작업자의 학생 관리 권한을 확인한다.
3. 완료 시 `is_completed`, `completed_by`, `completed_at`을 저장하고, 완료 취소 시 완료자와 완료 시각을 비운다.
4. 진행률은 `완료 Task 수 ÷ 전체 Task 수 × 100`을 정수로 반올림해 조회 시 계산한다.
5. Task는 최소 1개, 최대 50개이며 각 내용은 필수이고 최대 500자다.
6. 변경 전후 숙제·Task 스냅샷을 `HW_PROGRESS_UPDATED` 이력으로 저장한다.
7. 수동 진행률 변경 API와 0·25·50·75·100 선택 UI는 사용하지 않는다.
8. 화면 카드, 진행 바, 상태 요약은 서버 응답값으로 즉시 갱신한다.

## 5.4 숙제 삭제

1. 숙제 존재 여부와 작업자 권한을 확인한다.
2. 사용자에게 삭제 확인을 받는다.
3. 삭제 전 스냅샷을 저장한다.
4. `is_deleted`, `deleted_by`, `deleted_at`, `updated_by`, `updated_at`을 변경한다.
5. 삭제 후 스냅샷과 함께 `HW_DELETED` 이력을 저장한다.
6. 목록을 다시 조회해 삭제 숙제를 화면에서 제거한다.
7. 소프트 삭제된 숙제의 Task도 이력 추적을 위해 DB에 유지하지만 일반 숙제 조회에서는 함께 노출하지 않는다.

## 5.5 트랜잭션 및 오류

- 숙제와 이력 중 하나라도 저장 실패하면 전체 작업을 롤백한다.
- 권한 없음은 HTTP 403, 대상 없음은 HTTP 404, 입력값 오류는 HTTP 400으로 처리한다.
- 화면은 실패 시 기존 목록을 유지하고 사용자에게 재시도 메시지를 표시한다.

---

## 6. 화면 표출

### 6.1 목적

현재 관리 학생의 숙제를 선택 날짜, 상태 및 과목 기준으로 일관되게 표시한다.

### 6.2 초기 화면 진입

1. 현재 사용자와 관리 학생을 결정한다.
2. 관리 학생의 과목을 조회한다.
3. 관리 학생의 삭제되지 않은 전체 숙제를 조회한다.
4. 오늘 날짜에 숙제가 있으면 오늘을 선택한다.
5. 오늘 배정된 숙제가 없으면 `assignedDate` 기준 가장 가까운 예정 숙제 날짜를 선택한다.
6. 예정 숙제가 없으면 가장 최근 과거 숙제 날짜를 선택한다.
7. 선택 날짜가 포함된 주간 달력으로 이동한다.

> 검토 필요: 항상 오늘을 유지할지, 현재처럼 데이터가 있는 가까운 날짜로 자동 이동할지 최종 UX 정책을 확정해야 한다.

### 6.3 목록 필터 순서

```text
학생별 API 조회 결과
    → 선택 날짜 필터
    → 상태 필터
    → 과목 필터
    → 화면 카드 렌더링
```

- 학생 조건은 서버 API가 이미 적용하므로 화면에서 학생 ID로 중복 필터링하지 않는다.
- 선택 날짜는 `assignedDate === YYYY-MM-DD`로 비교한다.
- 상태는 진행률과 마감일을 이용해 계산한다.
- 과목 필터는 국어, 영어, 수학, 기타 네 가지를 제공한다.

### 6.4 상태 계산

| 조건 | 화면 상태 |
|---|---|
| 전체 Task 완료 | 완료 |
| 미완료 Task가 있고 마감일이 오늘 이전 | 기한 지남 |
| 일부 Task 완료 | 진행 중 |
| 완료 Task 없음 | 미완료 |

### 6.5 숙제 카드 표시

- 과목명
- 제목
- Task 체크리스트 펼침·접힘
- 마감일
- 진행률과 상태
- 생성자가 학부모인 경우 학부모 등록 표시
- 최근 수정 사용자와 수정 시각
- 수정·삭제 메뉴

### 6.6 데이터 최신화

- 사용자 전환 시 과목과 숙제를 새로 조회한다.
- 등록·수정·삭제 후 숙제 목록을 새로 조회한다.
- Task 완료 변경은 응답 데이터로 진행률과 목록을 즉시 갱신한다.
- 서로 다른 브라우저에서 동시에 변경한 데이터는 현재 실시간 반영되지 않는다.
- 필요 시 화면 포커스 복귀 재조회, 주기적 polling, SSE 또는 WebSocket 중 하나를 별도 정책으로 선택한다.

### 6.7 빈 상태와 오류 상태

- 선택 날짜에 숙제가 없으면 실제 선택 날짜를 메시지에 표시한다.
- 상태·과목 필터 결과가 없으면 필터 초기화 버튼을 제공한다.
- API 조회 실패 시 기존 사용자의 데이터를 남기지 않고 재시도 버튼을 제공한다.
- 연결 학생이 없는 학부모에게는 숙제 화면 대신 연결 안내를 제공한다.

---

## 7. API 기준표

| 액션 | 현재 API | 데이터 소유자 | 작업 수행자 |
|---|---|---|---|
| CSRF 토큰 발급 | `GET /api/auth/csrf` | 해당 없음 | 로그인 전후 브라우저 |
| 로그인 | `POST /api/auth/login` | 로그인 사용자 | 인증 성공 사용자 |
| 현재 사용자 조회 | `GET /api/auth/me` | 로그인 사용자 | 서버 세션 사용자 |
| 로그아웃 | `POST /api/auth/logout` | 로그인 사용자 | 서버 세션 사용자 |
| 테스트 사용자 조회 | `GET /api/test-users` | 해당 없음 | 인증 사용자, 향후 제거 예정 |
| 과목 조회 | `GET /api/students/{studentId}/subjects` | `studentId` | 서버 세션 사용자 및 승인 관계 검증 |
| 숙제 목록 조회 | `GET /api/students/{studentId}/homeworks` | `studentId` | 서버 세션 사용자 및 승인 관계 검증 |
| 숙제 등록 | `POST /api/students/{studentId}/homeworks` | `studentId` | 서버 세션 사용자 |
| 숙제 수정 | `PATCH /api/homeworks/{homeworkId}` | 기존 숙제의 학생 | 서버 세션 사용자 |
| Task 완료 변경 | `PATCH /api/homework-tasks/{taskId}/completion` | Task 소속 숙제의 학생 | 서버 세션 사용자 |
| 숙제 삭제 | `DELETE /api/homeworks/{homeworkId}` | 기존 숙제의 학생 | 서버 세션 사용자 |

모든 상태 변경 요청은 CSRF 토큰을 포함해야 한다. 인증되지 않은 요청은 HTTP 401, 학생 본인 또는 승인된 연결 학부모가 아닌 요청은 HTTP 403으로 처리한다.

---

## 8. 구현 변경 절차

1. 변경할 업무 규칙을 이 문서에서 먼저 수정한다.
2. 입력, 검증, DB 변경, 출력 및 오류 조건을 확정한다.
3. 필요한 DB 마이그레이션을 작성한다.
4. Repository, Service, Controller 순으로 서버 기능을 수정한다.
5. 화면 상태와 API 연동을 수정한다.
6. 학생 본인과 연결 학부모 시나리오를 함께 테스트한다.
7. 정상·권한 없음·대상 없음·입력 오류·DB 롤백 테스트를 수행한다.
8. 구현 결과와 검증 내용을 `workprocess.md`에 기록한다.

## 9. 현재 구현 완료 기능

| 영역 | 구현 내용 | 상태 및 비고 |
|---|---|---|
| DB 기반 사용자 | 학생·학부모 역할, 로그인 ID, 이름 및 계정 상태 저장 | 구현 완료 |
| 서버 세션 로그인 | BCrypt 비밀번호 검증, HTTP 세션 생성 및 세션 복원 | 구현 완료 |
| 로그인 보안 | CSRF 검증, 세션 ID 변경, HttpOnly·SameSite 쿠키 | 구현 완료 |
| 로그인 실패 제한 | 연속 실패 횟수 기록, 5회 실패 시 15분 잠금 | 기본 구현 완료, 운영 정책 재검토 가능 |
| 로그아웃·사용자 전환 | 서버 세션 무효화 후 로그인 화면 이동 | 구현 완료 |
| 사용자·학생 권한 분리 | 인증 사용자는 작업자, 학생 ID는 데이터 소유자로 처리 | 구현 완료 |
| 학생·학부모 권한 검사 | 학생 본인 또는 승인된 연결 학부모만 과목·숙제 접근 | 구현 완료 |
| 가족 연결 | 학생 코드 발급, 학부모 요청, 학생 승인·거절 및 양측 연결 해제 | 구현 완료 |
| 연결 없는 학부모 안내 | 로그인 세션을 유지하고 가족 연결 fragment로 이동 | 구현 완료 |
| 학부모 다중 학생 | 승인 학생 목록 응답, 기본 학생 선택 및 대시보드 전환 | 구현 완료 |
| 과목 구분 | 국어·영어·수학·기타 고정 분류 | 구현 완료 |
| 숙제 조회 | 선택일 기준 숙제 목록과 화면 상태 표시 | 구현 완료 |
| 숙제 등록·수정 | 선택일·마감일, Task 목록 및 완료 상태 보존 | 구현 완료 |
| 숙제 진행률 | 완료 Task 수와 전체 Task 수 기반 자동 계산 및 이력 저장 | 구현 완료 |
| 숙제 삭제 | 소프트 삭제 및 변경 이력 저장 | 구현 완료 |
| 화면 상태 | 날짜·상태·과목 필터, 로딩·빈 목록·오류 화면 | 구현 완료 |

## 10. 미구현·후속 기능 및 구현 API

### 10.1 기능 요약

| 우선순위 | 영역 | 필요한 기능 | 선행 조건 |
|---|---|---|---|
| 1 | 회원가입 | 역할 선택, 이름·로그인 ID·비밀번호 입력, 중복 검사, 학생 기본 과목 생성 | 가입 정책 확정 |
| 2 | 마이페이지 | 내 정보, 계정 상태, 최근 로그인, 비밀번호 변경 | 회원가입 및 사용자 API |
| 3 | 계정 복구 | 비밀번호 분실·초기화 및 잠금 해제 | 본인 확인 수단 결정 |
| 4 | 계정 종료 | 회원 탈퇴 또는 비활성화, 세션 종료, 데이터 보존 정책 | 탈퇴 정책 확정 |
| 5 | 테스트 기능 정리 | `/api/test-users`와 테스트 계정 안내 제거 | 실제 회원가입·관리 기능 완료 |
| 6 | 자동화 테스트 | 인증, CSRF, 권한, 관계 및 다중 학생 통합 테스트 | Docker/Testcontainers 환경 |
| 7 | 데이터 최신화 | 포커스 복귀 재조회, polling, SSE 또는 WebSocket 검토 | UX 요구 수준 결정 |

### 10.2 회원가입 예정 로직

1. 학생 또는 학부모 역할, 이름, 로그인 ID, 비밀번호와 비밀번호 확인을 입력한다.
2. 화면에서 필수값과 형식을 1차 검증하고 서버에서 다시 검증한다.
3. 로그인 ID의 대소문자 무시 중복을 검사한다.
4. 확정된 비밀번호 정책을 검사하고 BCrypt 해시만 저장한다.
5. 학생 가입 시 사용자 생성과 기본 과목 생성을 하나의 트랜잭션으로 처리한다.
6. 학부모 가입 시 로그인 상태를 유지한 채 학생 연결 안내 화면으로 이동한다.
7. 약관 동의가 필수라면 동의 버전과 동의 시각을 별도 데이터로 저장한다.

예정 API:

```http
POST /api/auth/signup
GET  /api/auth/login-id-availability?loginId=...
```

### 10.3 마이페이지 예정 로직

- 이름, 로그인 ID, 역할, 계정 상태와 최근 로그인 시각을 조회한다.
- 역할과 로그인 ID는 초기에는 변경 불가로 두고 이름과 비밀번호 변경부터 제공한다.
- 현재 연결된 사용자, 보낸 요청과 받은 요청을 한 화면에서 확인한다.
- 비밀번호 변경은 현재 비밀번호를 다시 검증하고 성공 시 기존 세션 유지 또는 전체 세션 종료 정책을 적용한다.
- 추후 회원 탈퇴와 계정 비활성화를 추가하며 숙제와 이력 보존 정책을 별도로 정의한다.

예정 API:

```http
GET   /api/users/me
PATCH /api/users/me
PATCH /api/users/me/password
```

### 10.4 학생·학부모 관계 설정 구현 API

관계 설정의 상세 업무 규칙은 3장을 기준으로 하며 현재 다음 API를 제공한다.

```http
GET    /api/family-links
POST   /api/family-links/invitations
PATCH  /api/family-links/{linkId}/approve
DELETE /api/family-links/{linkId}
POST   /api/family-links/invite-code/regenerate
```

거절과 요청 취소는 관계 이력을 삭제하지 않고 `DELETE /api/family-links/{linkId}`가 상태를 `LINK_DISCONNECTED`로 변경하는 방식으로 처리한다.

### 10.5 학부모 다중 학생 선택 구현 로직

1. 현재 사용자 응답의 단일 `student`를 승인된 `students` 목록으로 변경한다.
2. 연결 학생이 없으면 로그인 세션을 유지하고 관계 설정 안내 화면으로 이동한다.
3. 연결 학생이 한 명이면 해당 학생을 자동 선택한다.
4. 연결 학생이 여러 명이면 첫 학생을 기본 선택하고 대시보드 상단 선택기를 표시한다.
5. 대시보드 프로필 또는 상단 선택기에서 관리 학생을 바꿀 수 있게 한다.
6. 관리 학생 전환 시 숙제·과목·필터·작성 폼 상태를 초기화하고 새 학생 데이터를 조회한다.
7. 로그인 사용자는 계속 학부모로 유지하며 모든 API 요청에서 선택 학생과의 승인 관계를 서버가 다시 검사한다.

현재 사용자 응답:

```json
{
  "id": 3,
  "loginId": "parent-1",
  "role": "parent",
  "name": "학부모 이름",
  "students": [
    { "id": 1, "loginId": "student-1", "name": "학생 이름" }
  ]
}
```

## 11. 다음 단계 착수 전 결정 항목

- 회원가입 시 필수 정보와 비밀번호 최소 길이·복잡도
- 약관 동의 데이터의 저장 여부와 버전 관리 방식
- 연결 요청 만료 시간을 둘지와 만료된 요청의 표시·재요청 정책
- 마이페이지에서 이름과 로그인 ID 중 어떤 정보를 변경할 수 있는지
- 비밀번호 분실 시 사용할 이메일·휴대전화 등 본인 확인 수단
- 회원 탈퇴 시 사용자, 연결 관계, 숙제와 이력 데이터의 보존 방식
- 학부모 다중 학생 선택값을 브라우저에만 유지할지 서버 세션에도 유지할지
- 최초 대시보드 날짜를 오늘로 유지할지 가까운 숙제 날짜로 자동 이동할지
- 다른 브라우저 간 최신화 수준을 단순 재조회, polling, SSE 또는 WebSocket 중 어디까지 제공할지

---

## 12. 프런트엔드 화면 및 모듈 구조

### 12.1 분리 원칙

- 서비스는 현재와 같이 단일 페이지 안에서 로그인, 대시보드, 숙제 등록·수정 화면을 전환한다.
- `index.html`은 공통 문서 구조와 화면 fragment 조립만 담당한다.
- 화면별 HTML은 Thymeleaf fragment로 분리하고, 브라우저 동작은 ES module로 분리한다.
- API 통신, 공유 상태, 공통 유틸리티는 화면 모듈과 분리하여 중복 구현을 방지한다.
- 화면 모듈은 해당 화면의 이벤트를 등록하고, `app.js`가 전달한 화면 전환·렌더링 작업을 호출한다.

### 12.2 HTML 파일별 역할

| 파일 | 역할 |
|---|---|
| `templates/index.html` | 공통 `head`, CSS 연결, 화면 fragment 조립 및 JavaScript 진입점 로드 |
| `templates/fragments/login.html` | 로그인 안내, 로그인 ID·비밀번호 입력 폼과 오류 표시 |
| `templates/fragments/family-links.html` | 연결 없는 학부모 안내, 학생 연결 코드 및 요청·승인·해제 관리 |
| `templates/fragments/dashboard.html` | 주간 날짜 탐색, 사용자 메뉴, 상태·과목 필터와 숙제 목록 영역 |
| `templates/fragments/homework-form.html` | 숙제 등록·수정에 공용으로 사용하는 입력 폼 |
| `templates/fragments/dialogs.html` | 작성 취소·숙제 삭제 확인 대화상자, 모바일 가림막 및 토스트 |

### 12.3 JavaScript 파일별 역할

| 파일 | 역할 |
|---|---|
| `static/js/app.js` | 애플리케이션 초기화, 화면 전환, 데이터 조회와 렌더링 작업을 조정하는 진입점 |
| `static/js/api.js` | JSON API 요청, 동일 출처 쿠키, CSRF 헤더와 HTTP 오류 처리 |
| `static/js/state.js` | 현재 사용자, 선택 날짜, 필터, 숙제·과목 및 폼 상태 공유 |
| `static/js/utils.js` | DOM 조회, 날짜 계산·변환 및 HTML 이스케이프 공통 함수 |
| `static/js/screens/login.js` | 로그인 입력 검증, 인증 요청과 로그인 결과 처리 |
| `static/js/screens/family-links.js` | 연결 코드 입력·재발급, 연결 목록 렌더링 및 승인·거절·해제 이벤트 처리 |
| `static/js/screens/dashboard.js` | 날짜·필터·카드·Task 체크·프로필·모바일 메뉴 이벤트 처리 |
| `static/js/screens/homework-form.js` | 과목 선택, Task 추가·삭제·입력, 저장, 취소 및 작성 이탈 이벤트 처리 |

### 12.4 리소스 로딩 규칙

- `index.html`은 `/js/app.js`를 `type="module"`로 로드한다.
- 화면별 모듈은 공유 기능을 명시적으로 `import`하며 전역 변수에 의존하지 않는다.
- Thymeleaf가 서버에서 fragment를 조립한 후 기존 DOM ID를 유지하므로 CSS 선택자와 API 동작은 변경하지 않는다.
